
import { RegisterForm } from "@/features/auth/components/RegisterForm";
import { RegisterLayout } from "@/features/auth/components/RegisterLayout";

const Register = () => {
  return (
    <RegisterLayout>
      <RegisterForm />
    </RegisterLayout>
  );
};

export default Register;
